#!/usr/bin/env bash

# Test variable to verify direnv is working
export TEST_VAR="direnv_test"

# Load environment variables
set -a
source .env
set +a

# Load config if it exists
if [ -f .env.config ]; then
  set -a
  source .env.config
  set +a
fi
